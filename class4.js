var name = 'Nathaly';
var age = 26;

function printAge() {
    console.log(`${name} tiene ${age} años`);
}

printAge();

function printAgeArgs(n, e) {
    console.log(`${n} tiene ${e} años`);
}

printAgeArgs(name, age);